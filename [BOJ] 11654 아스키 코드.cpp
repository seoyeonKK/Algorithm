#include <iostream>
#include <string>
using namespace std;

char N;
int answer = 0;

int main () {
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    cin >> N;

    answer = N;

    cout << answer;
    return 0;
}
